import React from 'react';
import Layout from "./Layout";

const Root = () => {
    return (
        <Layout>
            <div>
                <h1>This is the Root Route</h1>
            </div>
        </Layout>
    );
};

export default Root;
